Just for docker run on centos6.3 kernel 2.6.32
  zpool support copy on write.
  Docker version is 1.3.0
Current status:
1. support Load image and run.
Need to Do
1. Meta Data storage for recover.
2. test other function just like pull
